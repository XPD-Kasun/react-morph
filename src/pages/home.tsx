import React from "react";
import {css} from "../../styled-system/css"

const Home = () => {
    return (
        <div className={css({background: 'green'})}>
            this is a test
        </div>
    )
};

export default Home;